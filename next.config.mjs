/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "img1.baidu.com",
            "cdnfile.sspai.com"
        ]
    }
};

export default nextConfig;
